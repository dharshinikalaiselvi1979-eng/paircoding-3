arr = [0, 1, 0, 3, 12, 0, 5]

# Remove all zeros
arr_no_zeros = [x for x in arr if x != 0]

# Count zeros
zeros_count = arr.count(0)

# Add zeros at the end
arr = arr_no_zeros + [0]*zeros_count

print(arr)
